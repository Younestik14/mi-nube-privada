import sqlite3

def init_db():
    # Al cambiar el nombre, obligamos a crear un archivo nuevo
    conn = sqlite3.connect('datos_estudiantes.db')
    c = conn.cursor()
    c.execute('''CREATE TABLE IF NOT EXISTS usuarios 
                 (nombre TEXT, num_estudiante TEXT PRIMARY KEY, estado TEXT)''')
    conn.commit()
    conn.close()
